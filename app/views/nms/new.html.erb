<!DOCTYPE html>
<html>
<head>
  <link rel="stylesheet" type="text/css" href="stylesheet/signin.css">
</head>
<body>
<div class="container login_martop">
<h2 class="form-signin-heading">请登录您的账户</h2>
<%= simple_form_for :pswd, html: { class: 'form-signin' }, url: nms_path do |f| %>
<h2 class="form-signin-heading"></h2>
  <%= f.input :name, placeholder: '用户名', :class => 'form-control' %>
  <%= f.input :pwd, placeholder: '密码', :class => 'form-control' %>
  <%= f.button :submit ,'登录', :class => 'btn btn-lg btn-primary btn-block' %>
<% end %>
      </form>

</div> 
</body>
</html>